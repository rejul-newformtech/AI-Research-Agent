services:
  server:
    build:
      context: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      # Persist SQLite database, ChromaDB embeddings, and documents
      - ./data:/app/data
      - ./logs:/app/logs
    restart: unless-stopped
